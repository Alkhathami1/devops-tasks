# Traceability

Every sub-requirement of the assignment, in the requester's wording, against
what was delivered and the evidence log that shows it.

| # | Requirement | Delivered | Evidence |
|---|---|---|---|
| **Task 1** — mount a volume from Windows to Linux |
| 1.1 | "protocol used and why" | SMB 3.1.1 chosen and justified against NFSv4, iSCSI and sshfs on the identity model; dialect `0x311` and an encrypted session read from the kernel's own record | `01-direction-a.log` |
| 1.2 | "mounting steps" | A real Windows share mounted on Linux over CIFS with `mount.cifs`, credentials in a mode-600 file, and a round trip verified by Windows reading back what Linux wrote | `01-direction-a.log` |
| 1.3 | "making it persistent across reboots" | An `/etc/fstab` entry for both shares with `_netdev`, `nofail` and `x-systemd.automount`, credentials in a mode-600 file beside a world-readable fstab, and `mount -a` at container start. Proven across two real reboots (`boot type 0x0`): the container returned on its restart policy and the entrypoint replayed the fstab entry, with the share readable through the restored mount and no manual mount command issued | `01-persistence.log`, `01-reboot.log` |
| 1.4 | "monitoring mount and disk performance" | `/proc/diskstats`, `iostat`, a `fio` load, node_exporter and Prometheus covering both the SMB share and the XFS mount, plus SMB session and reconnect counters exported from `/proc/fs/cifs` | `01-monitoring.log` |
| 1.5 | "alerting to guarantee service level" | Ten rules written against a stated SLO and validated by `promtool`; three driven inactive to pending to firing and back on live conditions, including the SMB share | `01-alerts.log` |
| 1.6 | "create a filesystem handling single files up to 1 TB, justified" | XFS chosen over ext4 and ZFS and built on a loop device; the kernel's single-file ceiling measured at 8 EiB, a 1 GiB file's extent map read across three allocation groups, and the filesystem grown online from 2 GiB to 4 GiB | `01-xfs.log` |
| 1.7 | "the reverse direction, Linux to Windows" | Samba serving SMB 3.1.1 with encryption required and SMB1 disabled, verified from the server. Executed end to end against the app-tier Samba server: a Windows client mapped the share, wrote a file, and the server read that file back on its own filesystem, with `Get-SmbConnection` reporting dialect 3.1.1 encrypted and `smbstatus` reporting SMB3_11 with AES-128-GCM | `01-direction-b.log`, `05-vpn-smb.log` |
| **Task 2** — multiple applications on a private Docker network |
| 2.0 | "multiple applications communicating over a private Docker network (database container, backend container, frontend + reverse proxy container)" | PostgreSQL, a Node backend and nginx across two networks, with the data tier marked `internal: true`; isolation proven by nineteen checks including nginx unable to resolve `postgres` | `02-network-isolation.log` |
| 2.1 | "automate as much as possible" | One command brings the stack to healthy — `docker compose up -d --wait` with healthchecks and `depends_on: service_healthy` — with migrations and seed running idempotently on startup | `02-stack-up.log` |
| 2.2 | "use a secret manager or environment variables" | Both mechanisms built side by side and contrasted: `docker inspect` shows the environment variable in plaintext and shows only a path for the file-based secret | `02-secrets.log` |
| 2.3 | "explain how the system recovers from unexpected failures (machine shutdown, RAM exhaustion, etc.)" | Five drills executed: a backend crash recovered by the restart policy, RAM exhaustion caught at `OOMKilled=true` t+1020ms with exit 137, a Docker daemon restart returning the stack unattended, the database volume surviving `down` then `up`, and the backend riding out a database outage without crash-looping | `02-drill-a-crash.log`, `02-drill-b-oom.log`, `02-drill-c-daemon-restart.log`, `02-drill-d-persistence.log`, `02-drill-e-dbfail.log` |
| 2.4 | "explain how CPU/storage/RAM are allocated" | CPU, memory and swap limits with reservations on all three services, read back from the daemon and measured under load: 2.25 CPU and 896 MiB of limits against a 12-CPU, 7.7 GiB VM | `02-resources.log` |
| **Task 3** — set up an MSSQL database |
| 3.0 | "set up an MSSQL database (managed or self-hosted)" | SQL Server 2022 self-hosted in Docker, pinned by tag and digest, healthy on 1433, with the self-hosted choice justified against Azure SQL and RDS on backup control | `03-stack-up.log` |
| 3.1 | "create a database" | `AppDb` created in FULL recovery with page checksums, reported ONLINE, and rebuilt from backup twice during the restore drills | `03-stack-up.log` |
| 3.2 | "create tables and insert data" | Four related tables with three foreign keys, seven check constraints and eleven indexes, seeded and then proven enforced by negative tests — an orphan insert rejected by `FK_orders_customer` | `03-schema.log` |
| 3.3 | "automated backups" | A supervised sidecar taking an unattended full backup at startup and running full, differential and log tiers on their own intervals with a retention pass; all three tiers captured with `RESTORE VERIFYONLY` and `WITH CHECKSUM`; the scheduler's own cadence recorded in `msdb.dbo.backupset` as six log backups five minutes apart with a differential among them | `03-backup-tiers.log`, `03-verify-suite.log` |
| 3.4 | "restore from a backup" | A full round trip after schema-level damage returning a byte-identical fingerprint, and a point-in-time recovery with `STOPAT` landing between two committed transactions — marker A present, marker B absent | `03-restore-roundtrip.log`, `03-point-in-time.log` |
| **Task 4** — copy a file between S3 buckets using multipart upload |
| 4.1 | "copy a file between S3 buckets using Multi-Part Upload" | A server-side `UploadPartCopy` engine driven from a Node-RED flow, with bounded concurrency and exponential backoff; the destination ETag suffix `-5` distinguishes it from the single-PUT source | `04-orphan-check.log`, `04-nodered-e2e.log` |
| 4.2 | "raise the upload size limit to the maximum" | Part size defaults to the 5 GiB S3 maximum and auto-raises when an object would exceed 10,000 parts, planned and asserted for 1 TiB and 5 TiB objects | `04-part-sizing.log` |
| 4.3 | "add appropriate logging" | One structured JSON line per event on a shared correlation id — `copy.start`, `source.inspected`, `plan.computed`, `upload.created`, `part.copied` per part, `copy.complete` — with byte ranges, durations and progress | `04-structured-logs.log` |
| **Task 5** — three networks, N-tier, segmentation, private VPN |
| 5.1 | "3 networks (public: nginx reverse proxy routing to the app; apps: an app server that reads from and writes to the database; dbs: the database)" | Three VPCs applied to real GCP infrastructure and configured by Ansible; a row written through nginx to the app to the database and read back | `05-architecture.log` |
| 5.2 | "3-tier/N-tier architecture" | Four instances across three tiers plus a bastion, two of four with a public address, the app and database tiers reaching the internet through Cloud NAT | `05-architecture.log` |
| 5.3 | "proper network segmentation" | Deny-by-default firewalls with service-account sources, and the tier boundary enforced at the routing layer: no route to the database range exists in the public VPC, proven three ways with a positive control | `05-architecture.log` |
| 5.4 | "access to the servers over a private VPN — especially the app and DB tiers" | WireGuard deployed on the bastion by Ansible with a generated keypair, and a client tunnel established from Windows: `wg show` reports a completed handshake with transfer counters nonzero in both directions, the client holds `10.99.0.2`, and the app tier answers ICMP and TCP 445 through the tunnel. Before the tunnel was raised the same address carried no adapter, no route and a refused connection on 445 | `05-vpn-smb.log` |
| 5.5 | "Terraform or any similar tool" | Terraform describes all 32 resources — three VPCs, peering, subnets, ten firewall rules, four instances, Cloud NAT and routers — validated, formatted and planned clean before apply | `05-plan.log` |
| 5.6 | Ansible configuration of the provisioned servers | Five roles applied through a dynamic inventory built from Terraform output, reaching private hosts by `ProxyJump` through the bastion, and idempotent on the second run at `changed=0` across all four hosts | `05-ansible-idempotency.log` |
| **Task 6** — OBS/vMix to AWS Elemental |
| 6.1 | "at least Full HD" | 1920x1080 encoded locally and again by MediaLive, measured with ffprobe on the segment AWS produced | `06-encode.log`, `06-s3-archive.log` |
| 6.2 | "at least 12 Mbps video and 192 kbps audio" | Encoder configured at 12 Mbps video and 192 kbps AAC; the local encode measures 12,652,330 bps overall with audio at 194,205 bps, and the MediaLive segment carries AAC at exactly 192 kb/s | `06-encode.log`, `06-s3-archive.log` |
| 6.3 | "optionally justify a different bitrate with an explicit BPP formula" | Bits per pixel computed rather than asserted across 1080p30, 1080p60, 4K30 and 4K60 at six bitrates; 12 Mbps at 1080p60 is 0.0965 bpp, mid-band for live HEVC | `06-analysis.log` |
| 6.4 | "HEVC codec" | H.265 Main profile with a 2-second GOP, encoded locally with libx265 and in AWS by MediaLive from an OBS Studio contribution feed, confirmed by ffprobe on the AWS-written segment as `hevc (Main)` with MPEG-TS stream type `0x24` at 60 fps | `06-s3-archive.log`, `06-obs.log` |
| 6.5 | "record the stream to S3 as .mxf or .ts" | A MediaLive ARCHIVE output group writing MPEG-TS to S3 over `s3ssl://`, chosen over MXF for segmentability and error resilience; 36 `.ts` segments written from the OBS feed, one pulled back and probed | `06-s3-archive.log` |
| 6.6 | "document every step" | Terraform describing all ten AWS resources validated and planned clean; the encoder profile documented field by field with the reason for each value, and the operator runbook written against the RTMP input that was deployed, installed from files so OBS reads the documented settings rather than having them typed in | `06-terraform-plan.log` |
